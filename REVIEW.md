# Review — Ian's first pass over the built decks #

**Ian, 2026-09-10, on the rendered Day 1 deck:** *"First, this a great first pass."* Then
twenty-six findings. This file is the **work list**, worked top to bottom. **§R1 and §R2 are closed; §R3 is Ian's
second pass over the rebuilt Day 1 deck.**

**Day 1 first, then a Day 2 pass** for the two findings that are not Day-1-specific — the text-box
split and the AI-isms. Ian reviews Day 2 by hand after that.

**§R1 and §R2's thirty rows closed 2026-09-10.** The last two were Ian's own: **R2-3** — Day 2 does
*not* get a blanket `#layout: side`, he names slides one at a time when he reviews it — and **R2-4** —
§*Putting It Together* drops *annotated* rather than growing ten new figures. **What R2-4 handed on
rather than fixed is in `BACKLOG.md` G1**: slides **138** and **141** carry ~4pt annotations, and **138**
and **144** ship typos.

**§R3 — Ian's second pass over Day 1, 2026-09-12. Sixteen rows, all applied, and Ian has called Day 1
closed**: *"With that, I suspect we close out the review on Day One."* Day 1 **136 → 129 slides**.
**⚑ Rows 1–14 are numbered against the 136-slide build; rows 15–16 against the 131-slide one.**

**§R4 — Ian's manual pass over Day 2, 2026-09-12. Fifty rows, none applied — this is the live queue.**
Numbered against the **153-slide** build at `d356745`. **§R4-0-1 is done — it closed R4-14 and R4-46 with it.** Three builder rows, two of
which answer seven of his content rows between them, and one of which reopens **Day 1** for a builder
fix. **Six rows are `☐` questions for Ian, not work** — R4-15, R4-34/35, R4-38, R4-45, and R4-0-2's
choice of fix.

> **Read `BACKLOG.md` for everything else that is outstanding.** This file is only this review.
> When a row here closes it closes here; `BACKLOG.md` §H carries one line pointing at this file.

---

## How to find a slide by its number ##

Ian numbers by the **1-based slide number in the built `.pptx`**, which is not the outline entry
number — 91 Day 1 entries become 138 slides, because 32 of them split into an argument and a
picture. The index is:

```bash
python3 tools/deck_index.py 1        # 1-based slide no. -> kind, section, title, figure
python3 tools/deck_index.py 1 --slide 63
```

**Every number below is a slide number in the deck as Ian reviewed it** — commit `495fd20`,
Day 1 at 138 slides. They will move as this list is worked, which is why every row also names its
**outline anchor**, and the anchor is what to trust.

---

## §R0 — the builder. Three of these are Ian's, one is mine ##

These are not content, and every one of them lands on **both** decks. Do these first: two of the
Day 1 content rows below cannot be answered until R0-2 and R0-4 exist.

| # | the ask | size | state |
|---|---|---|---|
| **R0-1** | **Slide numbers, bottom left.** *"Let's put numbers in the bottom left of slides, so we can easily refer to them."* A chrome op on every slide but the title slide, drawn like the kicker. **This is what makes every other row in this file addressable**, so it goes first | S | ✅ |
| **R0-2** | **One text box per block, and let it wrap.** *"On #3, I note that a paragraph is split into three text boxes. This will make it hard to add animation for progressive disclosure… We also don't want to have a single text box per page, because that also defeats progressive disclosure, we want text box per paragraph, bullet, or heading."* `emit_pptx` currently writes **one text box per wrapped LINE** with `word_wrap = False` — the comment says *"the layout already wrapped, and letting PowerPoint re-wrap would put the deck somewhere the preview is not"*. The **table cells already take the other option** and re-wrap to the measured width; bodies should too. **⚑ This is also the fix for all four of Ian's "overflows"** — see R0-3 | M | ✅ |
| **R0-3** | **The four overflows are one bug, and it is R0-2's.** #4, #61, #63 and #72 are **all callouts**, all Caveat, and **all four wrap correctly in the preview**. They run off the slide in PowerPoint because the box is `Inches(W_IN)` wide with `word_wrap = False`: a line that is 11.7in of Caveat is wider than the slide in whatever face PowerPoint substitutes (**`BACKLOG.md` F2 — the fonts are not installed on Ian's machine**). Fix R0-2 and these four fix themselves. **Verify all four after R0-2 rather than editing the four lines** | — | ✅ |
| **R0-4** | **Progressive disclosure, grouped by idea.** *"We also want animation for that disclosure (you should be able to edit this), but grouped by idea, not naively one transition per paragraph."* python-pptx has no animation API, so this is `<p:timing>` XML written into the slide part. **Ian ruled 2026-09-10: the builder infers it, no outline change.** A lead-in paragraph plus the bullet run under it is one step; a table, code block, quotation or callout is its own; the picture is its own. Built. It could not be checked here — no PowerPoint on this machine, and a malformed `p:timing` makes a file refuse to open rather than degrade. Every `spid` is asserted against a shape actually written, ids are unique and in document order, and both decks reopen; `--no-animation` turns it off. **Settled 2026-09-12: Ian read four slides out of the built deck, so it opens and the part is accepted.** Only the *behaviour* is unconfirmed — that a click advances one group, not one shape | L | ✅ |
| **R0-5** | **A fourth arrangement: text left, drawing right.** Not asked for by name, but **eight of Ian's rows need it** — #56/57, #58/59, #60/61, #62, #64/65, #68/69 — all the same instruction: *"shrink the diagram… put the text on the left"*. The builder has three arrangements today and the figure-leads one exists **because `styles.md`'s panel costs a drawing 47–63% of its label size**. Ian is overriding that for these slides knowingly. **Report what each one actually costs when the row is done**, per figure, and let him keep or revert it. **Built as `#layout: side`**, opt-in per entry, text 47% / drawing 53% of the content width. **What the six cost, and this is Ian's to keep or revert:** Dual-Write **49%**, Log Tailing **49%**, Invalid Message **69%**, Dead Letter **69%**, Outbox **71%**, State Change Capture **71%** — against 85–101% before. The two at 49% are wide 2021 imports fitted by width; the redrawn EIP pair hold up best | M | ✅ |

---

## §R1 — Day 1 content, in Ian's order ##

| # | slide(s) | outline anchor | the ask | size | state |
|---|---|---|---|---|---|
| **R1-1** | **#5** | `### Slide: Robust — Guaranteed Delivery` (:45) | *"'The other thing a boundary buys, and it is the cheaper of the two.' is stilted AI language, try to rewrite in my style. Broadly, try to remove AI-ism from text you generated as opposed to picked from original docs."* **Two jobs**: this line, and then a **sweep of Day 1 for prose that is ours rather than lifted**. `session-work/day1.txt` is the 2025 deck and is what "original docs" means — a line that appears there is his and stays | M | ✅ |
| **R1-2** | **#28 + #29** | `### Slide: Messaging and Events` (:301), `### Slide: Messaging vs. Eventing…` (:306) | *"On #28, I don't think this is doing enough work, it could be combined with #29. On #29, I think the comparison between Messaging and Eventing is better rendered as a table, see the original."* #28 is one sentence. Fold it in as the lead-in and **make #29 a table** | S | ✅ |
| **R1-3** | **#30** | `### Slide: Discrete vs. Series…` (:314) | *"I think Discrete vs Series would be better as a table."* Two bullets of paired attributes — they are a table pretending to be a list | S | ✅ |
| **R1-4** | **#31 + #32** | `### Slide: Message Types` (:322), `### Slide: Command / Document / Event Messages` (:327) | *"On #31, I don't think this is doing enough work and can be combined with #32."* Same shape as R1-2: #31 is one sentence naming the three, #32 defines them | S | ✅ |
| **R1-5** | **#41** | `### Slide: The Message Pump` (:419) | *"this was once visual, I think that works better than the text."* **It was**: 2025 deck slide 58 draws *Get → Translate → Dispatch → Handle* with the four error routes hanging off it (`session-work/day1.txt`). **A new figure**, and the error-routing prose becomes its labels | M | ✅ |
| **R1-6** | **#42** | `### Slide: Translate and Dispatch` (:433) | *"again this was once visual, I think it works better separate from #41 but needs to be visual."* 2025 deck slide 59 — the same four-stage pump with **Message Mapper Registry** and **Handler Registry** hanging off Translate and Dispatch. A second figure, deliberately **not** merged with R1-5's | M | ✅ |
| **R1-7** | **#47 → three slides** | `### Slide: Competing Consumers` (:494) | *"the split between how this works in streams and queues is full of AI-ism and badly described. We want three things."* **(a)** a general statement, and Ian wrote it: *"If the Rate of Arrival of messages (RoA) exceeds the Rate of Consumption (RoC) your channel will back up and the age of any message in the channel will increase. At some point the time a message waits in the channel will become unacceptable. You solve this problem by competing consumers, but the implementation differs between streams and queues."* **(b)** a queue slide with a diagram — *"locking (this item is being processed) and read-past, pick up the next item, but sacrifice ordering."* **(c)** a stream slide with a diagram — *"we partition by a key, and each partition has a consumer, and we retain ordering, but have no delay or read-past."* **⚑ Do R1-15 (§4.5) and this row together** — they are the same repetition seen from two ends | L | ✅ |
| **R1-8** | **#56 + #57** | `### Slide: The Dual-Write Problem` (:607) | *"shrink the diagram on #57 and combine with the text. It's too weird to read the words, then show the diagram here."* Needs **R0-5** | S | ✅ |
| **R1-9** | **#58 + #59** | `### Slide: Outbox` (:620) | *"move the text lines 'But look at what we just bought:' / 'We can fail after the send and before marking it Sent.' / 'The sweeper then finds it Pending and sends it again.' to presenter notes. Put the text on the left, and move the diagram from #59 here and shrink it… If needed move more of the text to notes to fit."* Needs **R0-5**. **⚑ The presenter note already says the duplicate is the question the Inbox answers** — check the move does not say it twice | S | ✅ |
| **R1-10** | **#60 + #61** | `### Slide: Log Tailing…` (:644) | *"move the diagram to the right on #60, text to the left on #60, shrink diagram, move some text to notes if needed. Split is weird."* Needs **R0-5**. The overflow half of this row is **R0-3** | S | ✅ |
| **R1-11** | **#62** | `### Slide: State Change Capture` (:674) | *"follow the changes on #58 and #60 and put diagram on the right (shrink) and text on left."* Already a single slide; it is the *arrangement* that changes. Needs **R0-5** | S | ✅ |
| **R1-12** | **#63 → two slides** | `### Slide: When the Handler Fails — Ack and Nack` (:686) | *"Split into two slides."* First keeps everything **through** *"…at-least-once stops being a slogan on the consumer side."* Second starts at *"But not acking is not a strategy, it is a question"* — **and is rewritten**, because Ian says the framing is wrong: *"On not ack we have options, not questions: **Requeue** ⇒ put this message back on the queue; **Reject** ⇒ don't process this message, drop it (either skip on a stream or delete on a queue). On a requeue we tend to set a policy for how many times, exceed that and you go to the DLQ. On a reject, if it is a badly formed message it goes to an invalid message queue. By policy we might send a well-formed message to the DLQ immediately on a reject."* **⚑ This changes what the next three slides are the answers to** — the `#note:` at :711 and the presenter note both say "three questions" and both need the same edit | M | ✅ |
| **R1-13** | **#64 + #65** | `### Slide: Invalid Message Channel` (:721) | *"Shrink the diagram, combine into one slide."* Needs **R0-5** | S | ✅ |
| **R1-14** | **#66, #67** | `### Slide: Requeue with Delay` (:739) | *"On #67, we should be clear that this is a queue, not a stream. On #66 we should note that only queues support requeue with delay via their locking mechanism. A stream must use another method to put a message onto the stream again, after a delay, such as a scheduler, and doing so will de-order the stream."* **The `#note:` at :752 already says "say queue here"** — Ian wants it *on the slide*, not in a build note. **New content**, and it is the stream half | S | ✅ |
| **R1-15** | **#68 + #69** | `### Slide: Dead Letter Channel` (:757) | *"Shrink the diagram, combine into one slide."* Needs **R0-5** | S | ✅ |
| **R1-16** | **#72** | `### Slide: What Your Broker Actually Gives You` (:794) | The overflow. **R0-3** — no content edit expected. **Verified 2026-09-10, all four**: each callout is now one box with `wrap="square"`, right edge **12.65in** on a 13.33in slide (Log Tailing's is 6.15, it is a `side` slide). No content was edited on any of the four | — | ✅ |
| **R1-17** | **§4.5, all of it** | `## 4.5 Queues and Streams` (:837) | *"there is repetition here, between the competing consumers and error discussions in slides above. Perhaps this is not a separate section, but needs to be combined into the material above as 'how this works for queues, how this works for streams', otherwise this will flow badly on the day by repeating ideas."* **The largest row in the file.** §4.5 is 18 slides and 11 figures; the overlap is with §4.3's *Competing Consumers* (R1-7) and §4.4's error slides (R1-12 … R1-15). **⚑ Structural — re-run the cross-reference sweep after it** (`BACKLOG.md` B6), and **`exercises/Quick-Start-Kafka.pptx` is wired to land in §4.5** (the `#note:` at :841), so a dissolved §4.5 has to say where the Kafka exercise goes. **Put the proposed shape to Ian before cutting** | L | ✅ |
| **R1-18** | **#94–#96** | `### Slide: Messaging Participants` (:978), `### Slide: Messaging or Eventing?` (:996) | *"This repeats the conversation from earlier, around #29. Again, let's combine into the earlier conversation."* #96's table **is** #29's table. Note the section goal at :975 and the figure `conversation-messaging-or-eventing.png` — decide what survives where | M | ✅ |
| **R1-19** | **#99–#111** | `### Slide: In-Only…` (:1029) through `### Slide: Choosing an Exchange Pattern` (:1181) | *"On #99-101 we describe three of the patterns. On #103 to #109 we redescribe the patterns. **We miss a whole pattern.** After the table, we should talk about each pattern, with all the information for it, and then at the end summarize error flows #102, and choices #110 and #111."* The missing one is **Out-In** — #99–101 are In-Only, Out-Only, In-Out. **Target shape:** the 2×2 (#97/98), then **one block per pattern with everything about it folded in** — In-Out absorbs #103 (timeout), #105 (command or query), #107 (blocking); Out-Only absorbs #106 (subscribe-notify); Out-In absorbs #108/109 — then **Faults, by Pattern** (#102), then **Choosing** (#110/111) | L | ✅ |
| **R1-20** | **#120** | `### Slide: Get It On Demand — REST/RPC` (:1298) | *"we are missing a diagram here, and we have diagrams for content enricher, let's put the diagram back."* A new `eip_figures` drawing — the consumer calling back for the data it was not sent | M | ✅ |
| **R1-21** | **#123** | `### Slide: Get It In Advance — ECST…` (:1333) | *"we are missing a diagram here and we have a diagram for content enricher, let's put the diagram back."* A second new drawing — the state arriving **on** the event, so there is no call back | M | ✅ |

---

## §R2 — the Day 2 pass ##

**Ian: *"Some of these points (such as text splitting and AI-ism) will apply to day two, but let's do
Day One first, then make a pass at Day Two for these issues before I manually review."*** So §R2
starts only when §R1 is closed, and it is deliberately **narrow** — it is not a second full review.

| # | the ask | size | state |
|---|---|---|---|
| **R2-1** | **Text splitting** — R0-1, R0-2 and R0-4 are builder changes and land on Day 2 for free. **Verify, do not assume**: render Day 2 and look, and re-check the callouts in particular, because that is where every Day 1 overflow was | S | ✅ |
| **R2-2** | **AI-ism** — the same sweep as R1-1, over `outlines/DayTwo.md`. `session-work/day2.txt` is the 2025 deck and is the test of what is Ian's | M | ✅ |
| **R2-4** | **⚑ Found while doing R2-2.** §*Putting It Together* (slides 130–145) promised an **annotation layer that is not in the artwork** — four titles ended *"— Annotated"* and four `#image:` alt lines said *"annotated with exchange patterns"*, and **not one of the ten figures carries a single pattern name.** Verified rather than inherited: **all ten appear exactly twice in `DayTwo.md`** — once where the flow is first taught (:188–:502), once here (:1177–:1211) — so an annotated version has to be a **new file**, and the sources are five `.excalidraw` and five `.drawio`, **neither of which this machine can render**, so *annotate* means *redraw in `tools/`*, i.e. G1. **Ian, 2026-09-10: drop *annotated*, leave it the spoken walk-through it already is.** Four titles renamed, five alt lines corrected — the fifth was *"composite … plus thumbnails of the other flows"* on slide 144, and **opening it showed one collaboration diagram and no thumbnails**, the same defect found the same way. R2-2's bodies already said only what is on screen and are unchanged. **⚑ What this does NOT fix, and G1 must:** plan §14.3 measures slides **138 and 141** at roughly 4pt, and **138 and 144 carry typos that ship today** | M | ✅ |
| **R2-3** | **Ian, 2026-09-10: do not roll it out — ask per slide.** Measured before asking, by building Day 2 twice: **38 entries split today, 23 carry exactly one figure** and so are eligible (`_side_slide` is guarded on one figure). Wholesale, the median figure goes **96% → 53%**, **all 23** land under 85%, and **7 need text cut to notes** (*Compensation, Four Ways* by 5.26in). **8 of the 23 are BPMN** — the one family already at the type floor (rule 6), where 49% is not a shrink. The two worst are *The Frame* (88→26%) and *The Desk* (101→29%), both because the slide also carries photographs, so the figure shares the half-stage at 3.0in not 6.1. **Day 2 keeps figure-leads. When Ian reviews Day 2 by slide he names the slides he wants text-left on, and each gets one `#layout: side` line** with the cost reported by name, as on Day 1 | — | ✅ |

---

## What this review did NOT ask for ##

Recorded so it does not get done by accident, and so the next session does not re-derive it.

- **Nothing about the 2021 imports' register** (`BACKLOG.md` G1). Five of them are the figures in
  R1-8 … R1-11 and R1-14, and Ian's instruction there is to **shrink and reposition**, not redraw.
  G1 stays scoped-and-not-started.
- **Nothing about `styles.md`.** The palette, the type scale and the two registers are not in scope.
- **No new sections and no cuts to §1, §2, §3, §6.1 or §6.3** beyond R1-1's prose sweep.


---

## §R3 — Ian's second pass over Day 1 ##

**2026-09-12, against the rebuilt deck.** Sixteen rows, in two batches — and **the two batches are
numbered against different builds**, which is exactly the trap `deck_index.py` exists for. R3-1…R3-14
were read off the **136-slide** deck; R3-15 and R3-16 off the **131-slide** rebuild. Day 1 is **129**
now. **Every row below is anchored on its entry title**, which is the only thing that did not move.

| # | slide (his) | the ask | state |
|---|---|---|---|
| **R3-1** | **7** | *"The opener said exactly this… That slide was the mechanism. This is what it bought you."* — AI-ism. Three sentences for one point, two of them about a previous slide | ✅ |
| **R3-2** | **10** | *"There is a second axis, and interacting between processes is what forces it on you."* — AI-ism. Announces an axis, then states it. Now *"The second axis is **time**."* | ✅ |
| **R3-3** | **14** | *"Hold that thought — the last slide of this section is about everything you did not get with it."* — AI-ism. A forward reference is presenter guidance; the entry had **no presenter note at all** and now has a specific one, checked against *Why Messaging* rather than paraphrased | ✅ |
| **R3-4** | **31 + 32** | *Queues Contain Tasks* — shrink and merge. *"Too hard to talk to the text, then repeat on the slide"* | ✅ |
| **R3-5** | **33 + 34** | *Streams Contain Facts* — merge, and drop four lines that are **on the image anyway**. **Three of the four are**: the offset store, the bookmark-on-restart sentence nearly verbatim, and *facts are an inverse database*. **The clause that is not** — *navigate the offsets to compute a point-in-time position* — went to the notes. The callout was not on the image either, but it **restated the two slide titles**, so it earned the cut | ✅ |
| **R3-6** | **50** | Text hidden under the picture. **⚑ Not a content row — see R3-14.** Ian quoted slide 58's callout against this slide; 50's own is *"Your handler is not a message handler…"*. The defect is real on both | ✅ |
| **R3-7** | **53 + 54** | *Competing Consumers on a Queue* — merge; *"Read-past buys you throughput. It spends your ordering to do it."* to the notes | ✅ |
| **R3-8** | **55 + 56** | *Competing Consumers on a Stream* — merge; *"On a queue you spend ordering to buy throughput…"* to the notes | ✅ |
| **R3-9** | **58** | The picture clips the callout. **R3-14** | ✅ |
| **R3-10** | **76** | The picture clips the callout. **R3-14** | ✅ |
| **R3-11** | **77 + 78** | *Inbox (Idempotency)* — shrink and merge | ✅ |
| **R3-12** | **101** | *"Drop. The other pattern slides are only text, and I don't think that this diagram adds."* `conversation-timeout` is now **placed zero times** — kept in `resources/` and in the family, not deleted | ✅ |
| **R3-13** | **111** | *"The original deck has a diagram for transitive dependencies… let's redraw and add it, probably on its own slide for space."* **It is Day 2 2025 slide 37, not Day 1** — and it has **no extracted image**, because the original is a native table plus text boxes and leader lines. Read out of the archived `.pptx` shape by shape and redrawn as `eip-fat-message-transitive` | ✅ |
| **R3-15** | **41 + 42** | *Translate and Dispatch* — *"Shrink the diagram and merge into one slide. Too hard to talk to separately."* **Numbered against the 131-slide build**, not the 136 one | ✅ |
| **R3-16** | **43 + 44** | *The Message Pump* — the same instruction. **The pair shares a spine**, so split they showed the same four-stage drawing four times in four slides. Nothing cut, nothing moved to notes: both fit beside their figure | ✅ |
| **R3-14** | **113** | The callout overlaps the prose. **⚑ This, R3-6, R3-9 and R3-10 are one number.** `_lines` reserved `n × pt × lead`, and the callout's `lead` is 1.06 against Caveat's 1.260em ink extent — so every callout claimed to end **0.200em before its descenders did**, and whatever came next sat 0.067in too high. **108 callouts across both decks**, 73 of them under an opaque picture. Fixed in `_reserve`; inter-line spacing is untouched. **Verified by Ian in the built deck, 2026-09-12** | ✅ |

### What §R3 turned up that Ian did not ask about ###

- **⚑ Every `#layout: side` slide reads at 8.9–12.8pt against an 18pt floor** — thirteen of them now,
  because the half-stage is **6.1in** where Phase 2 measured 12.4. Put to Ian with the figures.
  **Ian, 2026-09-12: *"I think that is fine."*** **So `#layout: side` is a standing exception to the
  floor**, now written into `styles.md` and `CLAUDE.md` rule 7 so it is not rediscovered as a defect.
  It is *not* a licence to put `side` on a figure the room must read on its own — the arrangement exists
  because the words sit beside the picture. **`reads_at.py` is still blind to it** and reports those
  figures at 18.0pt (`BACKLOG.md` **G12**); re-laying the four `qs-*` figures is **closed unstarted**
  (**G13**), since it existed only to reach a floor that no longer applies here.
- **The preview could not have shown R3-14**, by construction: it draws the lines itself at the same
  1.06, so it agreed with the layout and always had. That is the fourth thing the `.pptx` does that the
  preview cannot show.
- **R3-14 is verified. Ian checked slides 50, 58, 76 and 113 in the built deck, 2026-09-12** — the one
  change in this pass that could not be proved from the source. **Closed; do not raise it again.** It
  also retires the standing *does the deck open?* question behind R0-4: the file opens and PowerPoint
  accepts the `<p:timing>` part.

---

## §R4 — Ian's manual pass over Day 2 ##

**2026-09-12. Fifty rows, against the 153-slide Day 2 build at commit `d356745`.** The numbers were
checked against that build before anything was written down, and **every row below is anchored on its
entry title and outline line**, because the slide numbers move as soon as the first merge lands.

The anchor check that proves the build: Ian's row for **slide 9** is *"the image is not a frame, it is a
desk"*, and slide 9 really is `paper-the-desk.png` sitting under *The Frame*. His **slide 124** row and
his **slide 24** row turn out to be the same defect, which only lines up on this build.

```bash
python3 tools/deck_index.py 2            # SLIDE NUMBER -> section, title, figure
python3 tools/deck_index.py 2 --slide 81 # and one entry in full
python3 tools/side_cost.py 2 --slide 84  # what `#layout: side` WOULD cost that figure
```

**Where the callouts land.** On a split entry the `▎` callout renders on the **figure** slide, not the
argument slide — which is why Ian's callout rows name 54, 56, 60, 82 and 86 rather than 53, 55, 59, 81
and 85. Confirmed against the build on all five.

---

### §R4-0 — the builder. Three rows, and they answer seven of Ian's ###

Do these first. Two of them Ian reported from two different directions without knowing they were one
number, and the third gates four more rows.

| # | the ask | size | state |
|---|---|---|---|
| **R4-0-1** | **⚑ A wrapped table cell loses a space at every line break, and it lands on BOTH decks.** Ian reported it twice and they are one bug: **slide 24** *"some of the words run together, for example `knowwhat`"* and **slide 124** *"text overflows within the word on the first column"* — which is `State Machine` → `StateMachine` and `Workflow Engine` → `WorkflowEngine`, one long token in a narrow column. **The outline is correct** (`DayTwo.md:266` reads *"we need to know what we sent"*) and **the preview renders it correctly**, so this is a fifth `.pptx`-only defect. The cause is `build_deck.py:1018` — `flat = [t for line in row["cells"][ci] for t in line]` flattens the already-wrapped lines into one run sequence, and the wrapper has **already stripped the space at each break**. Verified in the XML: `<a:t>we need to knowwhat we sent</a:t>`. **Measured, not guessed: 286 table cells across both decks, 37 wrap to more than one line, 51 spaces lost** — 16 cells on Day 1 (slides 28, 72, 92, 97), 21 on Day 2 (slides 24, 124). **Fixed, and not in the flatten.** Re-inserting a space at each boundary is wrong — `wrap` breaks *before* the current word whether or not it had a space in front of it, and `**bold**, then` puts `bold` and `,` on one line with none, so a break there would write `bold ,`. `plan_table` now carries the **unwrapped** runs per cell as `flat` beside the wrapped `cells`, and `emit_pptx` writes those, which is what its own comment already said it did. **Verified: 37 of 37 wrapped cells correct in both built decks, 0 wrong**; `knowwhat` → `we need to know what we sent`, `StateMachine` → `State Machine`. The one survivor is `OrderStateMachine` in a C# listing on slide 121, verbatim from `DayTwo.md:1068`. **All 282 preview PNGs byte-identical**, which is the signature to want: the layout pass is untouched and only the `.pptx` changed | S | ✅ |
| **R4-0-2** | **A callout that PowerPoint wraps grows into a reserve computed for one line.** Ian's **slide 7** (*the Amis quote overlaps the prose*) and **slide 82** (*the callout is clipped by the diagram — "we fixed these on Day One"*). **Both clear in the preview and both clear in the layout model** — slide 7 reserves 0.534in between the callout baseline and the prose. So the error is in the `.pptx`, and **it is not `_first_baseline()`**: Caveat's `hhea` gives 0.960/−0.300, so its first-baseline lift at 24pt/1.06 is 0.808em against the 0.80em assumed — 0.008em, nothing. **The suspect is `BACKLOG.md` F2 — Caveat is not installed on Ian's machine**, so PowerPoint substitutes a wider face and the callout wraps to two lines. **Both of Ian's slides are in Day 2's top five for width headroom: slide 82 at 93.7% and slide 7 at 90.6% of the width they were given.** R0-2 set `word_wrap` on, which converted Day 1's *runs off the slide* into *grows downward* — so this is R0-3's residual, not a new defect. **Measured: 22 single-line Day 2 callouts, 13 at ≥75%; 26 Day 1, 12 at ≥75%.** ⚑ **Six of Day 2's worst thirteen are deleted by Ian's own rows** (56, 38, 20, 69, 86, 71), so land the content rows first and re-measure before choosing a fix. The options were F2 (install the fonts), slack in the reserve for a near-width callout, or shorter callouts. **Ian, 2026-09-12: install the fonts, plus a builder warning.** The install is his — `cp tools/fonts/*.ttf ~/Library/Fonts/`, and it fixes the *appearance* too, which no builder change can: a wider face is also the wrong face. **My half is built**: the builder re-wraps every callout at `SUBST_W` of its measure and names any that would need an extra line. **⚑ Calibrated on his two observations, not on a guess** — 0.92 caught slide 82 at 93.7% and *missed* slide 7 at 90.6%, so the substitute is ≥11% wider and `SUBST_W` is **0.90**. It names **12 callouts, 7 on Day 1 and 5 on Day 2**, both of his among them; **three of the five Day 2 ones are deleted by R4-12, R4-21 and R4-25**, which leaves two. Named by **entry**, not folio — `run()` numbers the deck after layout. Layout untouched: 129 Day 1 previews byte-identical | M | ✅ |
| **R4-0-3** | **A `#group:` has no divider slide, and four of Ian's rows ask for one.** *"We could do with a subsection header for A"*, and the same for **B**, **C** and **D** (R4-4, R4-16, R4-20, R4-27). **All four movements already exist** as `#group:` markers (`DayTwo.md:105, 301, 364, 554`) — but a group only sets the **kicker**, and `Deck.run()` calls `section_slide()` **once per `## Section`** and never for a group. So this is a new builder arrangement, and it gates the four rows: **cluster them.** Two decisions are Ian's. **(a) It must be visually subordinate** to a `##` divider, which is manila, a red 0.10in bar and a 40pt title — a movement divider that looks identical to *Flow and Reactive Programming* would read as a new section. **(b) It has to be opt-in per group, not automatic**: `DayTwo.md` has **six** groups, and the other two are *The Worked Flows — Just Paper Takeaway* (:177) and *The Worked Flows Again — as Graphs* (:460), which Ian has not asked to divide. ⚑ **R4-10's new framing slide may already be that group's divider** — decide the two together. **Built as the quiet card, Ian's choice of four**: paper ground, **no red bar, 29pt title** over a `carbon` Plex Mono eyebrow and one line of `comment`, against the section card's manila + red bar + 40pt. **Opt-in via a new `#divider:` line** under a `#group:` — `#group:` alone still only sets the kicker, and the divider text is the card's own line, so rule 2 holds. ⚑ **The eyebrow is carbon, not the muted the agreed mock-up said**: `styles.md` puts muted at 3.6:1 and reserves it for lines, not letters, and carbon is the four-colour rule's answer for an address. **Day 2 153 → 157 slides**; Day 1's 129 previews are byte-identical, and `deck_index` now names each card instead of printing a bare `[group]`. Specified in `styles.md` | M | ✅ |

---

### §R4 — Day 2 content, in Ian's order ###

**`side`** in the size column means the row needs `#layout: side`, the arrangement R2-3 settled is
**named per slide, never rolled out**. Ian has now named **eleven** pairs. Report what each costs its
figure by name when the row is done, as on Day 1.

| # | slide(s) | outline anchor | the ask | size | state |
|---|---|---|---|---|---|
| **R4-1** | **3** | `### Slide: Easy to Change, and Robust` (:15) | *"a little off now, as we don't talk to those four points."* **Ian supplied the replacement verbatim** — *"There are many drivers that cause us to distribute a system — performance and scalability, availability, maintainability, and applications that are inherently distributed. Strip the classification buckets away and the two properties you are actually buying are:"* Use his text, not a paraphrase of it. **Applied verbatim** but for a stray double space. **The bold on the four terms is kept** — he typed them plain, but that is prose in a message, and the outline bolds named terms everywhere; removing it was not asked for. ⚑ **The presenter note said *"Say the four reasons"*** and now says *the drivers*, or the slide and the note disagree | S | ✅ |
| **R4-2** | **4** | `### Slide: So How Do You Design One?` (:41) | *"has too much AI-ism"* — *"You already know the cost side. Temporal coupling multiplies outages; store and forward converts a failure into a delay. That trade is settled. What is not settled is what the system looks like when you take that trade seriously. That is today."* Four sentences, two of them about yesterday. **The `day2.txt` test settles which words are whose**: *temporal coupling* (5 hits) and *inherently distributed* are Ian's; *"the cost side"*, *"that trade is settled"* and *"converts a failure into a delay"* are all mine. ⚑ **And looking at the render found the real defect**: the paragraph and the callout directly under it both said *failure → delay*. My first rewrite still did. Only *temporal coupling multiplies outages* was unique, so the paragraph is now **two short sentences carrying just that**, and the callout asks the question once | S | ✅ |
| **R4-3** | **5 → 6** | `## Flow and Reactive Programming` (:67–72), `### Slide: Flow and Reactive Programming` (:92) | *"a wall of text, this would be better as the text on Slide 6 and as bullet points (progressively disclosed)."* The four movements are the section's italic blurb, which `section_slide()` sets at 18pt across 60% of the stage. Move them onto slide 6 as four bullets, one per movement, which the R0-4 grouping then discloses. **⚑ Slide 6's presenter note currently says *"Do not list the movements"* and *"It has to earn the room's attention in one line"*** — Ian is overriding that, so **the note has to change in the same edit** or the deck contradicts itself on one slide. **Done, and it needed a builder opt-in.** ⚑ The row above assumed *"the R0-4 grouping then discloses"* them — **it does not**: R0-4's rule folds a run of bullets with no lead-in into **one** click, so the four movements would have arrived together, the rule getting it wrong on the one slide Ian asked to disclose. **`#reveal: bullets`**, opt-in per entry exactly like `#layout:`, gives a click per bullet and leaves the inferred default alone, so this is not the outline annotating 286 slides. Slide 6 builds in **6 clicks** (callout, lead-in, four movements), verified in the `p:timing` — six click groups, every `spid` a real shape. Day 2 **221 → 226 clicks**. The bullets are deliberately **shorter than the four group cards' lines**, which is the pairing Ian chose the card option for; the section blurb is one line | M | ✅ |
| **R4-4** | **Movement A** | `#group: Movement A — How the Office Did It` (:105) | *"We could do with a subsection header for A."* Needs **R4-0-3**, and built with it — the card carries its movement in one line, drawn from the section blurb R4-3 moves off slide 5 | S | ✅ |
| **R4-5** | **7** | `### Slide: Paper Workflows` (:107) | The Amis callout overlaps the prose below it. *"Spacing might be a generic issue"* — **he is right that it is generic**: see **R4-0-2**. No content edit expected. **Now warned about at build time**; it clears when the fonts are installed | — | ☐ |
| **R4-6** | **8** | `### Slide: The Frame` (:122) | *"As well as the frame we have a picture of a mail cart… 'A mail cart picked up mail from the frame and distributed it.' 'If the frame is the broker, with topics, then the cart is the queue, distributing the messages to consumers.'"* **The asset is already wired** — `resources/photo-mail-cart.jpg` at :125 — and the slide's body never mentions it, so the photograph is on screen unexplained. Add the two lines. **This sharpened the existing callout rather than adding a second red**: *"That is a broker"* became Ian's own mapping — *"If the frame is the broker, with its topics, then the cart is the queue, distributing the messages to consumers."* A fourth bullet says the cart is what moves it, and the note now says to point at each photograph, because both finally earn their place | S | ✅ |
| **R4-7** | **9** | `### Slide: The Frame` (:122–127) | *"The image is not a frame, it is a desk."* **Confirmed** — `paper-the-desk.png` is the third `#image:` on *The Frame*, and the desk is not taught until the next entry. *"The original Slide 53 has two images, a skinny message — manilla envelope — and a fat message — a sack of folders (you have re-used on Slide 10). That is better as it calls back to Day One."* **Verified against the archive, and it is Ian's own slide**: 2025 Day 2 slide 53 carries two pictures labelled **Skinny** / **Fat**, with `Messaging/Discrete Event` / `Series Event (Document)` and `Discrete, Immediately Actioned` / `Series, Supports Action` — so the labels are his words and should survive. **Both assets exist**: `resources/Manila Envelope.jpg` (skinny) and `resources/Folder Stack.jpg` (fat). ⚑ **This closes `BACKLOG.md` F4** — `Manila Envelope.jpg` is currently linked nowhere. **And it moves `paper-the-desk.png`**, which must land on *The Desk* or be placed nowhere; say which. **Ian, 2026-09-12, on both halves.** (a) **The pair gets its own entry** — *Skinny and Fat, on Paper*, straight after *The Frame* — because both files are `.jpg` and the builder puts photographs in a panel beside text, never on a slide of their own, so "slide 9 becomes these two" would have crammed four photos into one panel. Day 1 §6.1 is *Fat and Skinny Messages*, so the callback uses the room's own words. ⚑ **`Manila Envelope.jpg` is only 202×203px** — reported, not assumed; it renders acceptably in a panel and is now placed, which **closes `BACKLOG.md` F4**. His 2025 discrete/series pairing is in the presenter note, not asserted on the slide, because Day 1 teaches the two axes separately. (b) **`paper-the-desk.png` is placed nowhere** — `paper-notation-key.png` on the very next slide **already draws a desk with in-tray, file and out-tray**, so placing it would have shown the same drawing twice running, which is the R3-16 defect. Kept in `resources/` and in `paper_flow`, like `conversation-timeout` | M | ✅ |
| **R4-8** | **10 → 11** | `### Slide: The Desk — In-Tray, Out-Tray, File` (:139) | *"Let's move the text to presenter notes on Slide 11 and drop this slide."* The body is the notation spec — three trays, then three rules. It becomes a presenter note behind `paper-notation-key.png`, which draws all of it. ⚑ **Rule 13 applies**: *every hand-off goes out-tray to in-tray* and *red dashed = paper moving* are **taught in a printed delegate brief**, so `grep -rn` across `exercises/` before this text stops being on a slide. **And the existing note already says the notation "has to be explicit now"** — reconciled into one note, not stacked. ⚑ **Rule 13 ran and paid**: the out-tray-to-in-tray rule and the three tray definitions are **independently in the printed delegate brief** (`Paper-Flow-Delegate-Brief.md:15, 30–32`), so the exercise is not stranded by the move. **And the check found a defect**: the brief's line 35 reads *"**dashed** / **solid** arrows | paper moving / a phone call or a fax"* with **no colour**, and the key draws the dashed arrow in **carbon** — so the outline's *"Red dashed arrows are paper moving"* contradicted its own next-slide figure. Corrected in the same edit; see R4-15 | M | ✅ |
| **R4-9** | **12** | `### Slide: Two Devices You Already Know` (:158) | *"let's drop it. Amusing but it interrupts the flow from notation to examples."* **⚑ Check what else points at it before cutting** — the order wheel is named as a forward reference (*"the wheel when queues come back"*) and the carbon copy is the outbox, which *How Do We Deal with Errors?* uses as a table row (*"make a carbon copy before you send"*). Rule 8: *cut means gone*, so it must not reappear as a note. **Orphans two assets** — `photo-order-wheel.jpg`, `photo-carbon-copy-pad.jpg` — see the asset ledger. **Checked before cutting, and it is safe**: the carbon copy is referenced downstream five times, but every one of them anchors on *How Do We Deal with Errors?*, not on this slide — `Paper-Flow-Facilitator-Guide.md:144` names that slide **by title** as the vocabulary source, and the brief says *"the vocabulary you saw ten minutes ago"*. The errors table introduces *make a carbon copy before you send → the outbox* on its own. Cut whole, both forward references in the note gone with it, nothing re-homed | S | ✅ |
| **R4-10** | **new, + 13, − 14** | `### Slide: Worked Flow — Restaurant Onboarding` (:179) | **Three edits.** **(a) A new slide inserted before 13**, and Ian supplied the whole thing — title *"Worked Example: Just Paper Takeaway"*, body *"We are going to demonstrate how a paper flow used to work. Our business produces a paper catalogue of local restaurant menus that we mail to nearby households. Customers phone us to order from the catalogue. We then pass the order to the restaurant. Restaurants handle their own delivery."* *"This frames the later slides."* — and **nothing in the deck currently states the business**. **(b) Drop slide 14**, the value stream: *"Slide 13 carries the weight… the value stream work is not referenced elsewhere."* **(c)** On the lead line, *"lose 'Just Paper Takeaway' and bold 'Signing up a new restaurant'"* — it becomes **`**Signing up a new restaurant**, as paper: the value stream first, then the flow`**, and **"the value stream first" must go too** once (b) lands. ⚑ **The presenter note says *"Walk the value stream first and the flow second, and say why"*** — that whole passage went with the figure. **All three applied**; the caption is now `**Signing up a new restaurant**, as paper.` and *"the value stream first, then the flow"* went with (b). ⚑ **R4-0-3's open question is answered by doing it**: Ian's slide is a titled content slide with five sentences of body, which is not a divider card, so `The Worked Flows` gets **no** `#divider:`. **But his title duplicated the group kicker** — both read *Just Paper Takeaway* — so the group is renamed to `The Worked Flows` and the slide carries the business name alone | M | ✅ |
| **R4-11** | **16, − 17** | `### Slide: Worked Flow — Customer Order` (:197) | *"Slide 16 carries the weight, drop 17, the value stream work is not referenced elsewhere."* Same cut as R4-10(b). The body's first bullet was *"The value stream for the order side of the business, end to end"* and went with it | S | ✅ |
| **R4-12** | **20** | `### Slide: Worked Flow — Order Placement` (:219) | *"Drop the text: 'Store, send, and be able to pick it up again. Two hundred years before anyone called it a saga.'"* The callout. ⚑ **It is the entry's only red**, and the presenter note leans on the same idea (*"Say file it before you send it"*) — so the note carries it after the cut and nothing is lost. Also **one of the 13 near-width callouts** (90.9%), so it helped R4-0-2 — it has dropped out of the build warning. The note already says *"Say file it before you send it"*, so nothing was re-homed | S | ✅ |
| **R4-13** | **23** | `### Slide: Worked Flow — Order Confirmation` (:239) | **⚑ The largest row in §R4, and it is a figure job.** *"Originally the goal here was the animation. I added arrows to the diagram, colour-coded for synchronous and asynchronous communication and progressively showed the arrows so that the flow could be seen. For this reason it did not matter that the scale was small. We should try to restore this."* Today it is one static `paper-worked-flows-montage.png`. Restoring it needs **the arrows drawn as layers and disclosed by R0-4's `<p:timing>`** — which today groups by outline block, not by parts of one picture, so **the builder cannot animate inside a figure**. **Establish the original before drawing**: find the arrow colours and the reveal order in the archived 2025 `.pptx` rather than inventing them (rule 4 — open it, do not assume). **Ian's note that scale stopped mattering is the design licence here**, and it interacts with R4-49, which points the *Full Flow* slides back at this same montage | L | ☐ |
| **R4-14** | **24** | `### Slide: How Do We Deal with Errors?` (:258) | *"Some of the words in the text run together, for example 'knowwhat'."* **R4-0-1** — a builder bug, and the outline is already correct. No content edit. **Closed by the builder fix**: the cell now reads *we need to know what we sent* | — | ✅ |
| **R4-15** | **25–27** | `### Slide: How Do We Deal with Errors?` (:258) | *"Perhaps we should lose the red, as we have B&W elsewhere, or use red on the others?"* **A question, not an instruction — put both options to him with the pictures.** The three are 2021 `.drawio` imports (`Restaurant Onboarding Errors`, `Customer Order Errors`, `Order Placement Errors`), so this is **`BACKLOG.md` G1 territory and this machine cannot render `.drawio`** — recolouring means redrawing. ⚑ **Rule 13 first**: *red dashed arrows are paper moving* is line 35 of a printed delegate brief, so red in this family may be **taught**, not decorative, and "lose the red" could contradict the exercise. `grep -rn` across `exercises/` before answering. **`tools/repaint_paper_reds.py` exists** and is the tool that already did this once — read it before starting | M | ☐ |
| **R4-16** | **Movement B** | `#group: Movement B — How Would You Build That?` (:301) | *"We could do with a section header for Movement B."* Needs **R4-0-3**, and built with it — the card carries its movement in one line, drawn from the section blurb R4-3 moves off slide 5 | S | ✅ |
| **R4-17** | **31 + 32** | `### Slide: Call and Return, and the God Object` (:317) | *"Shrink the diagram and combine into a single slide. Remove the text: 'Somebody has to be in charge, and in call and return it is always main.'"* **Applied. `flow-call-and-return.png` 87% → 66%**, text fits beside it, nothing moved to notes | side | ✅ |
| **R4-18** | **33 + 34** | `### Slide: SOA Is OO at Macro Scale` (:331) | Merge, and drop the Josuttis quote — *"'A service should represent a self-contained functionality that corresponds to a real-world business activity.' — Nicolai Josuttis, SOA in Practice"*. ⚑ **`flow-soa-service.png` is already one of Day 2's three figures under 85%** (81% at 8.6in), so `side` makes an existing shortfall worse — **and it does: 81% → 58%, the lowest figure in either deck.** Ian's to keep or revert. ⚑ **And looking at the render answered the quote question**: the Josuttis words are **lettered into the drawing itself**, so the callout was the R3-5 case — the slide saying twice what the picture already said once. **So it is dropped, not moved**; the drawing carries no *attribution*, so the presenter note supplies the name and the `#note:` now records why it must not come back as text | side | ✅ |
| **R4-19** | **35 + 36** | `### Slide: Feature Envy — You Built a Distributed Monolith` (:350) | *"Shrink the diagram and combine into a single slide."* Nothing cut. **Applied. `flow-entity-services.png` 101% → 81%** — the best-holding of the three | side | ✅ |
| **R4-20** | **Movement C** | `#group: Movement C — The Formalism` (:364) | *"We could do with a section header for Movement C."* Needs **R4-0-3**, and built with it — the card carries its movement in one line, drawn from the section blurb R4-3 moves off slide 5 | S | ✅ |
| **R4-21** | **37 + 38** | `### Slide: Data Flow Programming` (:366) | Merge, and drop *"In call and return, control moves and data sits still. In dataflow, data moves and control sits still."* ⚑ **That line is the movement's thesis** and the second-widest callout in the deck (95.3%) — **move it to the notes rather than losing it**, and say so, unless Ian means it gone | side | ☐ |
| **R4-22** | **39 → 40** | `### Slide: Nodes, Ports and Firing` (:381) | *"Make Slide 39 Presenter Notes on Slide 40."* The whole argument becomes a note behind `flow-node-ports.png` + `flow-two-nodes.png`. **Two figures, so `_side_slide`'s one-figure guard rules `side` out here** — this is a different move from R4-17 and is why Ian asked for it differently | M | ☐ |
| **R4-23** | **42 → 43** | `### Slide: Capacity, Backpressure and Node Lifetime` (:400) | *"Make Slide 42 Presenter Notes on Slide 43."* Same as R4-22, one figure (`flow-arc-buffers.png`) | M | ☐ |
| **R4-24** | **54** | `### Slide: Worked Example — Onboarding in FBP` (:462) | *"Lose the text: 'Nothing here was invented. It was named.'"* The callout | S | ☐ |
| **R4-25** | **56** | `### Slide: Worked Example — the Order Flow in FBP` (:487) | *"Lose the text: 'Three flows on paper. One graph. The composition was always there — paper just could not draw it.'"* **The widest callout in the deck** (95.7%), so it also retires the worst R4-0-2 case | S | ☐ |
| **R4-26** | **60** | `### Slide: Worked Example — When It Fails…` (:510) | *"Lose the text: 'You have just drawn a distributed system.'"* ⚑ **The presenter note calls the next beat *"the hinge into movement D"* and says *"do not let the failure discussion eat it"*** — that hinge is this line. Check the note still makes sense once it is gone | S | ☐ |
| **R4-27** | **Movement D** | `#group: Movement D — The Name` (:554) | *"We need a section header for Movement D."* Needs **R4-0-3**, and built with it — the card carries its movement in one line, drawn from the section blurb R4-3 moves off slide 5 | S | ✅ |
| **R4-28** | **63 → 64** | `### Slide: Reactive Architectures` (:556), `### Slide: The Reactive Manifesto` (:563) | *"Let's combine the single point from 63 into 64; it looks orphaned alone."* Slide 63 is one line — *"Reactive derives from **reactive programming**, not from OO"* — and becomes 64's lead-in. Same shape as Day 1's R1-2 and R1-4. **Its presenter note has to merge too, not be dropped** | S | ☐ |
| **R4-29** | **65** | `### Slide: The Reactive Manifesto` (:578) | *"'Two properties, one mechanism. Somebody wrote it down in 2014.' AI-ism"* | S | ☐ |
| **R4-30** | **66** | `### Slide: Reactive Traits — Value, Form, Means` (:593) | *"AI-ism"* — the attribution passage, *"This is a gloss, not manifesto text… The split is how Bonér has presented it in talks, and it is a defensible reading — say so on the slide rather than letting it read as a quotation."* ⚑ **It is hedging written to be read, and it is also load-bearing**: the value/form/means split really is not in the manifesto. **Rewrite it short, do not delete the caveat** — one line on the slide, the rest in the notes | S | ☐ |
| **R4-31** | **68 + 69** | `### Slide: Microservices Are Reactive Architectures` (:617) | Merge, and drop *"This morning called independent deployability the prize. This is how you stop giving it back."* ⚑ **"This morning" is a Day 2 forward/back reference** — check it against what Day 2 actually says before assuming the line is only an AI-ism | side | ☐ |
| **R4-32** | **70 + 71** | `### Slide: Partitioning and Dataflow` (:631) | Merge, and drop *"Divide by verb, not by noun. Entity services divide by noun — that is Feature Envy."* — *"This text is repeated on the diagram."* **Verify that on the PNG before cutting** (rule 1): this is exactly the R3-5 shape, where three of four lines were on the image and the fourth was not, and only the fourth needed the notes | side | ☐ |
| **R4-33** | **72 + 73** | `### Slide: Bulkheads` (:644) | Merge, and **move** (not drop) *"You met this on Day 1 §4.4. Store and forward is what makes it a delay."* ⚑ **A §-number cross-reference into Day 1 — and Day 1 has just been restructured to 129 slides, §4.5 dissolved.** `BACKLOG.md` B6 is the sweep for exactly this: **check §4.4 still says what this claims** before moving the line | side | ☐ |
| **R4-34** | **74 + 75** | `### Slide: When the Pipe Fills — Backpressure or Load-Shedding` (:659) | *"Let's swap the order."* **⚑ Ambiguous, and it must be settled with R4-35 in one question, not guessed.** Two readings: **(a)** swap the *two slides* so `flow-when-the-pipe-fills.png` leads and the argument follows — a figure-first arrangement the builder does not have; **(b)** swap the *entry* with *Putting Reactive Together*, which is what R4-35 asks for from the other side, making this row and the next one row. **(b) is the likelier reading** — his next line is *"Slide 76 should be first before 74/75"* — but ask | S | ☐ |
| **R4-35** | **76 → before 74** | `### Slide: Putting Reactive Together` (:680) | *"Should be first before 74/75. It sets out the set to review."* Move the entry up so the overview precedes the mechanisms. **Cluster with R4-34, R4-36 and R4-37** — all four are the same four slides, and done piecemeal they fight | S | ☐ |
| **R4-36** | **77** | `### Slide: Putting Reactive Together` (:680) | *"This is really Circuit Breaker and not Putting Reactive Together."* `flow-circuit-breaker.png` splits out as its **own titled entry**. With R4-37 this empties *Putting Reactive Together* of both its figures, leaving it the overview R4-35 wants it to be — which is why these are one job | M | ☐ |
| **R4-37** | **78** | `### Slide: Putting Reactive Together` (:680) | *"This is really Scale up, not Down and not Putting Reactive Together. In the notes we should call back to Competing Consumers."* `flow-scale-out.png` becomes its own entry. **⚑ Ian wrote "Scale up" and the figure is `flow-scale-out`** — scale *out* is what competing consumers do, and his own callback names them, so confirm the title word. Day 1's Competing Consumers is the R1-7 trio, rebuilt this week: **get the current slide numbers from `deck_index.py 1` rather than citing the old ones** | M | ☐ |
| **R4-38** | **81** | `### Slide: Your Flow, in the Standard Notation` (:724) | **⚑ The most important row in §R4 — it is a correctness fix on the section's key takeaway, not a polish.** The mapping table has two rows the wrong way round. Ian: *"'a dashed arrow between an out-tray and an in-tray' is 'a sequence flow' because it is 'inside the process boundary' which is the desk, whereas 'a numbered step from one desk to the next inside the bar' is the 'message flow' because it is across process boundaries (i.e. someone picks it up in the mail cart). So the text is wrong here. This is important as 'sequence flow' is an orchestration and message flow is choreography. They are often confused, and this is the key takeaway. Let's adjust the text, and add some Presenter notes."* **Confirmed the table currently reads the other way** (:733–734). ⚑ **One thing to settle with him before editing**: the same table also says *"the heavy vertical bar → a **pool** boundary"*, and if a **desk** is the process boundary then the pool boundary cannot be only the heavy bar — **the third row has to move with the two he named**, or the slide contradicts itself. Ask with the three rows written out. **Then check the downstream slides say the same thing** — *Process = Orchestration* (:832), *Collaboration and Choreography* (:887) and R4-41's dropped callout all restate this distinction | M | ☐ |
| **R4-39** | **82** | `### Slide: Your Flow, in the Standard Notation` (:724) | *"is clipped by the diagram (we fixed these on Day One)."* **R4-0-2** — he is right that it is the Day 1 defect, and right that it is not fixed. No content edit. **Now warned about at build time, and it clears when the fonts are installed** | — | ☐ |
| **R4-40** | **83 + 84** | `### Slide: BPMN` (:746) | *"Shrink the diagram on Slide 84 and combine with Slide 83 into one slide."* ⚑ **BPMN is the one family already at the type floor** (rule 6), and R2-3 measured the eight BPMN `side` candidates at ~49% — *"where 49% is not a shrink"*. `bpmn-ordering-flow.png`. **Report the cost by name; Ian has ruled the `side` exception acceptable but he has not seen it on BPMN** | side | ☐ |
| **R4-41** | **85 + 86** | `### Slide: BPMN — The Elements` (:758) | Merge, and drop *"Sequence flow is what happens at a desk. Message flow is what happens between desks."* **⚑ Do this AFTER R4-38, not before** — that callout is a statement of the very distinction R4-38 is correcting, and on Ian's reading *"at a desk"* / *"between desks"* is the **right** formulation. Cutting it first destroys the evidence for the fix | side | ☐ |
| **R4-42** | **95 + 96** | `### Slide: Process = Orchestration` (:832) | *"Shrink the diagram on Slide 96 and combine with Slide 95 into one slide."* BPMN again — see R4-40. `bpmn-shopping-as-sequence.png` | side | ☐ |
| **R4-43** | **110** | `### Slide: Messaging and Eventing (Orchestration vs. Choreography)` (:936) | *"Retitle to Misunderstandings: Choreography and Orchestration."* ⚑ **Ian's spelling is "Choreograpy"** — a typo in the instruction, not the intended title. **And the title feeds the kicker** (`_kicker_warned` clips a long one silently, rule 11) — check the length | S | ☐ |
| **R4-44** | **111** | `### Slide: Event Chaining` (:944) | *"Retitle to Mistakes: Event Chaining."* With R4-43 this makes a pair — *Misunderstandings:* then *Mistakes:* — so **do them together and keep the prefixes parallel** | S | ☐ |
| **R4-45** | **114 ↔ 115–119** | `### Slide: Tentative Operations` (:972), `### Slide: Durable Execution` (:984) | *"Swap, as Durable Execution is needed by all the activity types including tentative."* **Move *Tentative Operations* after the block he named** — Durable Execution (115), Activities and Resources (116), its Worked Example (117), Handlers + Activity State Updates (118–119). ⚑ **Confirm exactly where Tentative lands**: his reason is that durable execution underpins *every* activity type, but the activity-type run continues past 119 into State Machine (120–121), Routing Slip (122) and Workflow Engines (123), so *after 119* and *after 123* are both consistent with what he wrote. **Ask with the resulting running order written out**, and re-check the presenter notes for forward references either way | M | ☐ |
| **R4-46** | **124** | `### Slide: Compensation, Four Ways` (:1111) | *"Text overflows within the word on the first column."* **R4-0-1** — `State Machine` and `Workflow Engine` are being written as one token each. **Not an overflow and not a content edit**; the row closes when the builder is fixed. ⚑ **Do not shorten the cells** — that is the R1/§R3 trap where four attempts at a table overflow achieved nothing because the wrong cell was measured. **Closed by the builder fix**: column one now reads *State Machine* and *Workflow Engine*, two tokens each, and nothing was shortened | — | ✅ |
| **R4-47** | **− 125, → 120** | `### Slide: Compensation, Four Ways` (:1111), `### Slide: State Machine + Activity State Updates` (:1054) | *"Drop. Mention that a saga is usually a state machine in the notes to Slide 120."* Drops `bpmn-compensation-fragment.png` from the deck. **⚑ Check the figure is not placed anywhere else before calling it orphaned** — the `bpmn_shopping`/`bpmn_hotel` families place several figures twice (`bpmn-shopping-collaboration.png` is on 97, 103 and 105), so *"placed once"* has to be proved, as it was for R3-12's `conversation-timeout` | S | ☐ |
| **R4-48** | **126 + 127** | `### Slide: Workflow Engines — Embedded, External…` (:1128), `### Slide: Smart Endpoints, Dumb Pipes` (:1138) | *"Slide 127 is really a name for the danger in Slide 126, so move it there. Make Smart Endpoints, Dumb Pipes a quote, and add the text below. Result is one slide."* So: one entry, whose **callout is the quotation** *"Smart endpoints, dumb pipes"* and whose body is 126's ESB/SOA argument. ⚑ **Attribute the quote** — it is Fowler and Lewis on microservices, and R4-30 is Ian objecting to exactly this kind of unattributed gloss. **`styles.md`'s one-idea-in-red rule**: the merged slide gets one callout, so check whether 126 already has one | M | ☐ |
| **R4-49** | **141 + 142** | `### Slide: Full Flow, End to End` (:1197) | *"Slide 141 and Slide 142 are not the full flow. Full paper flow is on Slide 23. Full FBP flow is Slide 58. OK to repeat here."* **He is right and it is checkable**: 141 is `Order Placement.drawio.png` and 142 is `flowbased_order_placement.png` — **one flow of four, on a slide titled *Full Flow, End to End***. Repoint at `paper-worked-flows-montage.png` (slide 23) and `flowbased_order_all.png` (slide 58). ⚑ **Ian's *"OK to repeat here"* is the permission that matters** — R2-4 established every *Putting It Together* figure appears exactly twice, so this keeps that invariant, and the **placed-exactly-once check has to be re-run after the row**. **Interacts with R4-13**: if the montage becomes an animated layered figure, decide whether the static form still exists for this slide | M | ☐ |
| **R4-50** | **143–145** | `### Slide: Putting It Together — Recap` (:1205) | *"and redundant once we fix full flow."* The recap is `Order Confirmation.drawio.png` + `flowbased_order_all.png` — and R4-49 moves `flowbased_order_all.png` onto 142, so the recap then shows a figure the previous slide just showed. **Depends on R4-49; do them together.** ⚑ **This is the last content slide of the section** — check what the *Next Steps* divider follows, and whether the section needs any closing slide at all | M | ☐ |

---

### The asset ledger — what §R4 does to `resources/` ###

**The deck regenerates from `outlines/` + `resources/` + `tools/`, so a row that moves a picture has to
say where the file goes.** Nothing below is done; this is the list to keep true as rows close.

| row | asset | what happens |
|---|---|---|
| **R4-6** | `photo-mail-cart.jpg` | **already linked** (`DayTwo.md:125`) — the row adds the words, not the file |
| **R4-7** | `Manila Envelope.jpg` | **newly placed** — closes `BACKLOG.md` **F4**, which has it linked nowhere |
| **R4-7** | `Folder Stack.jpg` | **moves** from *The Desk* (:154) to *The Frame*, or is placed twice — say which |
| **R4-7** | `paper-the-desk.png` | **displaced.** Must land on *The Desk* or be recorded as placed nowhere, like `conversation-timeout` |
| **R4-9** | `photo-order-wheel.jpg`, `photo-carbon-copy-pad.jpg` | **orphaned** by dropping slide 12 — two new F4s unless re-placed |
| **R4-10, R4-11** | `Restaurant Onboarding Value Stream.drawio.png`, `Order Flow Value Stream.drawio.png` | **orphaned** — *"the value stream work is not referenced elsewhere"* |
| **R4-13** | `paper-worked-flows-montage.png` | **redrawn as layers**, and R4-49 also wants the static form. Decide whether one file or two |
| **R4-15** | the three `* Errors.drawio.png` | **recolour = redraw** (G1) — no `.drawio` renderer here |
| **R4-47** | `bpmn-compensation-fragment.png` | **orphaned** — prove it is placed once before saying so |
| **R4-49** | `Order Placement.drawio.png`, `flowbased_order_placement.png` | **dropped from slide 141/142**; both still placed in Movement A/C, so neither is orphaned |
| **R4-50** | `Order Confirmation.drawio.png` | **orphaned** if the recap goes |

**None of these is a new drawing except R4-13**, and **none is a new photograph** — every asset R4 needs
is already in `resources/`. `photo-manila-folders.jpg` is linked nowhere either way and is a second
candidate for R4-7's fat message; **open both before choosing** (rule: `ls | grep`, never guess).

---

### What §R4 turned up that Ian did not ask about ###

- **⚑ His slide 24 and slide 124 rows are one builder bug, and it is on Day 1 too.** 51 lost spaces
  across 37 wrapped table cells in both decks (**R4-0-1**). Day 1's review is closed, so this reopens
  Day 1 for a builder fix — Ian should know that before it happens. The same *one number answers several
  rows* shape as R0-3 and R3-14.
- **⚑ The callout clip he reports on slide 82 as *"we fixed these on Day One"* is not fixed, and it is
  not R3-14.** R3-14 was the reserve, and the reserve is now right. This is **F2** — the fonts are not
  on his machine, PowerPoint substitutes a wider face, and R0-2's `word_wrap` turned *runs off the
  slide* into *grows into the slide below*. **13 Day 2 callouts and 12 Day 1 callouts** sit at ≥75% of
  the width they were given; **his two are at 93.7% and 90.6%**. **Six of Day 2's thirteen are deleted
  by his own rows**, so the cheapest order is content first, then re-measure (**R4-0-2**).
- **The four movement headers need a builder arrangement that does not exist**, and it has two
  decisions in it that are his (**R4-0-3**). `#group:` sets a kicker; only `## Section` makes a slide.
- **`flow-soa-service.png` is already at 81%** before R4-18 puts it on a half-stage, and **three
  Day 2 figures are under 85% today** — the other two are `flowbased_order_food_errors.png` (79%) and
  `bpmn-the-six.png` (81%). R4-18, R4-40 and R4-42 all land on figures that are already short.
- **Eleven `side` pairs is close to doubling the count** — 13 today across Day 1, and R2-3's measurement
  said all 23 Day 2 candidates wholesale would put the median at 53%. Eleven is not 23, but **the
  running median should be reported as they land**, not at the end.
- **Three rows are genuinely ambiguous and are clustered into two questions**, not guessed:
  R4-34/R4-35 (what "swap" means, and it decides R4-36/R4-37 with it) and R4-45 (where *Tentative
  Operations* lands). **R4-38 has a third**: the `pool` row of the table has to move with the two he
  named or the slide contradicts itself.
- **R4-38 is a correctness fix, not a polish**, and R4-41 deletes the evidence for it — so the order
  matters. Ian says the sequence/message flow confusion *"is the key takeaway"*, which makes it the
  highest-value row in the file.
